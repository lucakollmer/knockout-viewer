/// <reference lib="webworker" />
import {ASETS_ENGINE_VERSION,ASETS_SCHEMA_VERSION,CancelledError,createSearchMetrics,effectiveFamily,type DownsetRecord} from '../asetsCore';
import {createFamilyGeometryContext,geometryRecordCached} from '../asetsGeometry';
import {buildFastModulusContext,iterFastDownsets,type FastModulusContext} from '../asetsFast';
import type {AsetsComputeRequest,AsetsFamilyChunk,AsetsFamilyHeader,AsetsFamilyKey,AsetsGroupTransform,AsetsPerformance,AsetsWorkerMessage,AsetsWorkerRequest} from '../asetsProtocol';
const CACHE_DB='knockout-asets-cache';const CACHE_DB_VERSION=1;const HEADER_STORE='asetFamilyHeaders';const CHUNK_STORE='asetFamilyChunks';const TRANSFORM_STORE='asetGroupTransforms';const PROGRESS_RECORD_INTERVAL=64;const CACHE_CHUNK_SIZE=64;const MODULUS_CONTEXT_CACHE_LIMIT=3;
let activeGeneration=0,activeRequestId=0;let queuedRequest:AsetsComputeRequest|null=null;let running=false;const modulusContexts=new Map<number,FastModulusContext>();let cachePromise:Promise<IDBDatabase|null>|null=null;
const familyKeyArray=(key:AsetsFamilyKey):IDBValidKey[]=>[...key];const chunkKeyArray=(key:AsetsFamilyKey,i:number):IDBValidKey[]=>[...key,i];const post=(m:AsetsWorkerMessage)=>self.postMessage(m);
function openCache():Promise<IDBDatabase|null>{if(!('indexedDB'in self))return Promise.resolve(null);if(cachePromise)return cachePromise;cachePromise=new Promise(resolve=>{const req=indexedDB.open(CACHE_DB,CACHE_DB_VERSION);req.onerror=()=>resolve(null);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(HEADER_STORE))db.createObjectStore(HEADER_STORE);if(!db.objectStoreNames.contains(CHUNK_STORE))db.createObjectStore(CHUNK_STORE);if(!db.objectStoreNames.contains(TRANSFORM_STORE))db.createObjectStore(TRANSFORM_STORE)};req.onsuccess=()=>resolve(req.result)});return cachePromise;}
async function idbGet<T>(storeName:string,key:IDBValidKey):Promise<T|null>{const db=await openCache();if(!db)return null;return new Promise(resolve=>{const tx=db.transaction(storeName,'readonly');const req=tx.objectStore(storeName).get(key);req.onerror=()=>resolve(null);req.onsuccess=()=>resolve((req.result as T|undefined)??null);});}
async function idbGetMany<T>(storeName:string,keys:readonly IDBValidKey[]):Promise<Array<T|null>>{const db=await openCache();if(!db)return keys.map(()=>null);return new Promise(resolve=>{const tx=db.transaction(storeName,'readonly'),store=tx.objectStore(storeName);const values:Array<T|null>=keys.map(()=>null);let failed=false;keys.forEach((key,i)=>{const req=store.get(key);req.onerror=()=>{failed=true};req.onsuccess=()=>{values[i]=(req.result as T|undefined)??null}});tx.onerror=()=>resolve(keys.map(()=>null));tx.oncomplete=()=>resolve(failed?keys.map(()=>null):values);});}
async function idbPutCompleteFamily(key:AsetsFamilyKey,chunks:readonly AsetsFamilyChunk[],header:AsetsFamilyHeader,transform:AsetsGroupTransform|null):Promise<void>{const db=await openCache();if(!db)return;const stores=transform?[CHUNK_STORE,HEADER_STORE,TRANSFORM_STORE]:[CHUNK_STORE,HEADER_STORE];await new Promise<void>(resolve=>{const tx=db.transaction(stores,'readwrite');const cs=tx.objectStore(CHUNK_STORE);for(const chunk of chunks)cs.put(chunk,chunkKeyArray(key,chunk.chunkIndex));tx.objectStore(HEADER_STORE).put(header,familyKeyArray(key));if(transform)tx.objectStore(TRANSFORM_STORE).put(transform,transform.groupId);tx.onerror=()=>resolve();tx.oncomplete=()=>resolve();});}
const emptyPerformance=():AsetsPerformance=>({cacheHit:false,modulusContextSetupMs:0,candidateCspEnumerationMs:0,geometryMs:0,totalWorkerComputeMs:0,serializationChunkingMs:0,indexedDbReadMs:0,indexedDbWriteMs:0,peakUsedJsHeapBytes:null});
function heapBytes():number|null{const memory=(performance as Performance & {memory?:{usedJSHeapSize?:number}}).memory;return Number.isFinite(memory?.usedJSHeapSize)?Number(memory?.usedJSHeapSize):null;}
function updatePeakHeap(perf:AsetsPerformance):void{const value=heapBytes();if(value===null)return;perf.peakUsedJsHeapBytes=Math.max(perf.peakUsedJsHeapBytes??0,value);}
const makeHeader=(key:AsetsFamilyKey,r:number,residues:readonly[number,number,number]):AsetsFamilyHeader=>({schemaVersion:ASETS_SCHEMA_VERSION,engineVersion:ASETS_ENGINE_VERSION,familyKey:key,r,residues,status:'computing',downsetTotal:0,coherentTotal:0,noncoherentTotal:0,chunkCount:0,normalizedResultDigest:null,completedAt:null,performance:null});
function assertActive(token:number){if(token!==activeGeneration)throw new CancelledError();}
async function readCompleteFamily(request:AsetsComputeRequest,key:AsetsFamilyKey,certificate:ReturnType<typeof effectiveFamily>['certificate'],token:number):Promise<boolean>{const start=performance.now();const header=await idbGet<AsetsFamilyHeader>(HEADER_STORE,familyKeyArray(key));let readMs=performance.now()-start;assertActive(token);if(!header||header.schemaVersion!==ASETS_SCHEMA_VERSION||header.engineVersion!==ASETS_ENGINE_VERSION||header.status!=='complete')return false;post({type:'status',requestId:request.requestId,phase:'cache',familyKey:key,certificate,emittedRecords:header.downsetTotal});if(request.includeRecords){const keys=Array.from({length:header.chunkCount},(_,i)=>chunkKeyArray(key,i));const chunkStart=performance.now();const chunks=await idbGetMany<AsetsFamilyChunk>(CHUNK_STORE,keys);readMs+=performance.now()-chunkStart;assertActive(token);let emitted=0;for(let i=0;i<chunks.length;i++){const chunk=chunks[i];if(!chunk)return false;emitted+=chunk.records.length;post({type:'chunk',requestId:request.requestId,familyKey:key,chunkIndex:i,records:chunk.records,cached:true});}if(emitted!==header.downsetTotal)return false;}const cachedHeader={...header,performance:header.performance?{...header.performance,cacheHit:true,indexedDbReadMs:readMs}:null};post({type:'complete',requestId:request.requestId,familyKey:key,certificate,header:cachedHeader,cached:true});return true;}
function makeTransform(request:AsetsComputeRequest,key:AsetsFamilyKey,certificate:ReturnType<typeof effectiveFamily>['certificate']):AsetsGroupTransform|null{return request.groupId?{schemaVersion:ASETS_SCHEMA_VERSION,engineVersion:ASETS_ENGINE_VERSION,groupId:request.groupId,familyKey:key,certificate,updatedAt:new Date().toISOString()}:null;}
async function computeAndCache(request:AsetsComputeRequest,token:number){const normalized=effectiveFamily(request.r,request.residues);const key=[ASETS_ENGINE_VERSION,normalized.r,...normalized.residues] as AsetsFamilyKey;const perf=emptyPerformance();const wall=performance.now();post({type:'status',requestId:request.requestId,phase:'cache',familyKey:key,certificate:normalized.certificate,emittedRecords:0});if(await readCompleteFamily(request,key,normalized.certificate,token))return;post({type:'status',requestId:request.requestId,phase:'context',familyKey:key,certificate:normalized.certificate,emittedRecords:0});let ctx=modulusContexts.get(normalized.r);if(ctx){modulusContexts.delete(normalized.r);modulusContexts.set(normalized.r,ctx);}else{const t=performance.now();ctx=buildFastModulusContext(normalized.r,()=>token!==activeGeneration);perf.modulusContextSetupMs=performance.now()-t;modulusContexts.set(normalized.r,ctx);if(modulusContexts.size>MODULUS_CONTEXT_CACHE_LIMIT){const oldest=modulusContexts.keys().next().value;if(oldest!==undefined)modulusContexts.delete(oldest);}}assertActive(token);const geometryContext=createFamilyGeometryContext(normalized.r,normalized.residues);let header=makeHeader(key,normalized.r,normalized.residues);post({type:'status',requestId:request.requestId,phase:'compute',familyKey:key,certificate:normalized.certificate,emittedRecords:0});const metrics=createSearchMetrics();const iterator=iterFastDownsets(normalized.r,normalized.residues,{modulusContext:ctx,cancelCheck:()=>token!==activeGeneration,metrics});let recordCount=0,coherent=0,chunkIndex=0;let chunk:DownsetRecord[]=[];const chunks:AsetsFamilyChunk[]=[];const flush=()=>{if(!chunk.length)return;const records=chunk;chunk=[];const stored:AsetsFamilyChunk={schemaVersion:ASETS_SCHEMA_VERSION,engineVersion:ASETS_ENGINE_VERSION,familyKey:key,chunkIndex,records};chunks.push(stored);if(request.includeRecords){const t=performance.now();post({type:'chunk',requestId:request.requestId,familyKey:key,chunkIndex,records,cached:false});perf.serializationChunkingMs+=performance.now()-t;}chunkIndex++;};while(true){assertActive(token);const tc=performance.now(),next=iterator.next();perf.candidateCspEnumerationMs+=performance.now()-tc;if(next.done)break;const tg=performance.now();const record=geometryRecordCached(next.value,normalized.residues,normalized.r,geometryContext);perf.geometryMs+=performance.now()-tg;recordCount++;chunk.push(record);if(record.coherent)coherent++;if(recordCount%PROGRESS_RECORD_INTERVAL===0){post({type:'status',requestId:request.requestId,phase:'compute',familyKey:key,certificate:normalized.certificate,emittedRecords:recordCount});updatePeakHeap(perf);}if(chunk.length>=CACHE_CHUNK_SIZE)flush();}flush();post({type:'status',requestId:request.requestId,phase:'finalize',familyKey:key,certificate:normalized.certificate,emittedRecords:recordCount});perf.totalWorkerComputeMs=performance.now()-wall;updatePeakHeap(perf);header={...header,status:'complete',downsetTotal:recordCount,coherentTotal:coherent,noncoherentTotal:recordCount-coherent,chunkCount:chunkIndex,completedAt:new Date().toISOString(),performance:perf};const tWrite=performance.now();await idbPutCompleteFamily(key,chunks,header,makeTransform(request,key,normalized.certificate));perf.indexedDbWriteMs+=performance.now()-tWrite;header={...header,performance:{...perf}};post({type:'complete',requestId:request.requestId,familyKey:key,certificate:normalized.certificate,header,cached:false});}
async function runLoop(){if(running)return;running=true;try{while(queuedRequest){const request=queuedRequest;queuedRequest=null;activeRequestId=request.requestId;const token=activeGeneration;try{await computeAndCache(request,token)}catch(error){if(error instanceof CancelledError||token!==activeGeneration)post({type:'cancelled',requestId:request.requestId});else post({type:'error',requestId:request.requestId,message:error instanceof Error?error.message:String(error)});}}}finally{running=false;if(queuedRequest)void runLoop();}}
self.onmessage=(event:MessageEvent<AsetsWorkerRequest>)=>{const message=event.data;if(message.type==='cancel'){if(message.requestId===undefined||message.requestId===activeRequestId){activeGeneration++;queuedRequest=null}return;}activeGeneration++;queuedRequest=message;void runLoop();};
